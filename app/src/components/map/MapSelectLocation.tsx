import React, { useState } from 'react';

import { MapSelectLocationProps } from '../../types/mapSelectLocationComponentProps';

export const MapSelectLocation: React.FC<MapSelectLocationProps> = (props) => {
  return <div>Map not availalble on web</div>;
};
