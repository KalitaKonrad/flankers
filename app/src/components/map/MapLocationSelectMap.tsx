import React, { useState } from 'react';

import { MapLocationSelectMapProps } from '../../types/mapSelectLocationComponentProps';

export const MapLocationSelectMap: React.FC<MapLocationSelectMapProps> = (
  props
) => {
  return <div>Map not availalble on web</div>;
};
